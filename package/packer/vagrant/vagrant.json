{
  "variables": {
    "password": "rancher",
    "version": "v0.8.0",
    "checksum": "e33cff33b89dab8ea0c390aaa1dc4aa1c512a44e9cd5aa9ad465c9cc87f1cf3e"
  },
  "builders": [{
    "type": "virtualbox-iso",
    "boot_wait": "6s",
    "boot_command": [
      "<enter>",
      "<wait10>",
      "rancher",
      "<enter>",
      "sudo k3os install",
      "<enter>",
      "1",
      "<enter>",
      "y",
      "<enter>",
      "http://{{ .HTTPIP }}:{{ .HTTPPort }}/cloud-config.yml",
      "<enter>",
      "y",
      "<enter>"
    ],
    "guest_os_type": "Linux_64",
    "http_directory": ".",
    "iso_url": "https://github.com/rancher/k3os/releases/download/{{user `version`}}/k3os-amd64.iso",
    "iso_checksum": "{{user `checksum`}}",
    "iso_checksum_type": "sha256",
    "ssh_username": "rancher",
    "ssh_password": "{{user `password`}}",
    "ssh_wait_timeout":"1000s",
    "disk_size": "8000",
    "shutdown_command": "sudo poweroff",
    "post_shutdown_delay": "10s",
    "format":"ova"
  }, {
    "type": "qemu",
    "boot_wait": "6s",
    "boot_command": [
      "<enter>",
      "<wait20>",
      "rancher",
      "<enter>",
      "sudo k3os install",
      "<enter>",
      "1",
      "<enter>",
      "y",
      "<enter>",
      "http://{{ .HTTPIP }}:{{ .HTTPPort }}/cloud-config-qemu.yml",
      "<enter>",
      "y",
      "<enter>"
    ],
    "http_directory": ".",
    "iso_url": "https://github.com/rancher/k3os/releases/download/{{user `version`}}/k3os-amd64.iso",
    "iso_checksum": "{{user `checksum`}}",
    "iso_checksum_type": "sha256",
    "ssh_username": "rancher",
    "ssh_password": "{{user `password`}}",
    "ssh_wait_timeout":"1000s",
    "disk_size": "8000",
    "shutdown_command": "sudo poweroff",
    "format": "qcow2"
  }],
  "provisioners":[
    {
      "type": "shell",
      "execute_command": "{{ .Vars }} sudo -E sh '{{ .Path }}'",
      "inline": ["rm -rf /var/lib/rancher/k3s","echo hello world"]
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "keep_input_artifact": false,
      "output": "k3os_{{.Provider}}.box",
      "vagrantfile_template": "vagrantfile.template"
    }
  ]
}
